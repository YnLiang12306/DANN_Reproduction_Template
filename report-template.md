# DANN 跨被试运动想象分类实验报告

## 1. 任务说明

本实验复现 DANN（Domain-Adversarial Training of Neural Networks），实现运动想象脑电的跨被试无监督域适配 4 分类。

```text
源域（带标签）：8 名被试的运动想象 EEG
目标域（无标签）：1 名被试的运动想象 EEG（标签仅用于最终评估）
输出：目标域 4 类运动想象（左手 / 右手 / 双脚 / 舌头）
评估：目标域测试集 accuracy + Cohen's Kappa + 混淆矩阵（9 折平均）
```

**核心学习目标**：理解域偏移与对抗训练，掌握梯度反转层（GRL）机制，把 EEGNet / EEGConformer 改造成跨被试可迁移模型。

## 2. 数据集与域设置

- 数据集名称：BCI Competition IV 2a
- 范式：运动想象（Motor Imagery），4 分类
- 通道数：22，采样率：250 Hz，被试数：9
- 每被试训练集 trial 数：288，测试集 trial 数：288
- 域适配设置：leave-one-subject-out（LOSO），共 9 折
- 使用设备：CPU / GPU

### 域划分表

| 角色 | 组成 | 样本数 | 是否用标签训练 |
|:---:|:---:|:---:|:---:|
| 源域（Source） | 8 名被试训练集 | 2304 | 是 |
| 目标域-训练（Target train） | 1 名被试训练集 | 288 | 否 |
| 目标域-测试（Target test） | 1 名被试测试集 | 288 | 仅评估 |

### 被试与类别信息

| 类别 | 标签 | 含义 |
|:---:|:---:|:---:|
| 0 | left_hand | 左手运动想象 |
| 1 | right_hand | 右手运动想象 |
| 2 | feet | 双脚运动想象 |
| 3 | tongue | 舌头运动想象 |

请说明你的源域/目标域具体如何构造（例如源域是否只用了训练集、是否做了数据增强以扩大源域、源域和目标的采样是否均衡）：

```text
（在这里填写）
```

## 3. 数据预处理

| 预处理 | 参数设置 |
|:---:|:---:|
| 频带滤波（Bandpass Filter） | |
| 时间窗口截取 | |
| 归一化 | |
| 验证集划分 | |
| 数据增强（如有） | |
| ... | |

请说明这些预处理对源域和目标域是否**完全一致**（域适配要求两个域使用相同的预处理，否则会人为引入额外的域差异，干扰域判别器的判断）：

```text
（在这里填写）
```

## 4. 模型结构

### 4.1 整体架构图

画出你的 DANN 完整结构，标注每层的输入输出维度：

```text
e.g.

Input EEG: (batch, 1, C=22, T=?)
  │
  ▼
[Feature Extractor G_f]  （EEGNet / EEGConformer 去掉分类头）
  │   f = G_f(x) ∈ R^d，d = ?
  │
  ├────────────────────────────────────┐
  ▼                                    ▼
[Label Predictor G_y]            [Gradient Reversal Layer GRL]
  │   FC(d -> 256) -> ReLU            │  前向：恒等 R(f)=f
  │   FC(256 -> 4)                    │  反向：×(-λ)
  ▼                                    ▼
L_y = CE(ŷ, y_s)   (仅源域)     [Domain Classifier G_d]
                                      │   FC(d -> 256) -> ReLU
                                      │   FC(256 -> 2)
                                      ▼
                                   L_d = CE(d̂, d)  (源+目标)
```

### 4.2 关键维度参数

| 参数 | 符号 | 你的取值 |
|:---:|:---:|:---:|
| 特征提取器 | G_f | EEGNet / EEGConformer |
| 特征向量维度 | d | |
| 标签预测器隐层维度 | — | |
| 域判别器隐层维度 | — | |
| 域判别器层数 | — | |
| λ 退火参数 | γ | 10 |
| 模型总参数量 | — | |

### 4.3 各组件作用说明

| 组件 | 在 DANN 中的作用 | 你的理解 |
|:---:|:---:|:---:|
| Feature Extractor G_f | 提取既判别（可分 4 类）又域不变（分不清被试）的特征 | |
| Label Predictor G_y | 用特征 f 做 4 类运动想象分类，只在源域上训练 | |
| Domain Classifier G_d | 判别特征来自源域还是目标域，驱动对抗 | |
| Gradient Reversal Layer | 反向传播时把域判别损失的梯度取反，形成 min-max 博弈 | |

### 4.4 尝试回答以下关于 DANN 的问题

请在报告中逐一回答（不需要长篇大论，说清楚即可）：

1. **为什么跨被试分类是「域适配」问题？** 源域和目标域的分布差异具体体现在 EEG 的哪些方面？
2. **GRL 前向和反向分别做了什么？** 为什么不能直接给 $L_d$ 加负号，而要用一个专门的层？
3. **DANN 的 min-max 目标在「对抗」什么？** 平衡点在哪里？域判别器如果太弱或太强会怎样？
4. **λ 为什么要退火？** 如果一开始就把 λ 拉满会有什么问题？
5. **「域不变特征」和「可判别特征」会不会冲突？** DANN 如何在这两者之间取得平衡？
6. **为什么能直接把 EEGNet/EEGConformer 的 backbone 当作 G_f？** 它们的哪些性质适合做跨被试特征提取？

```text
（在这里逐一回答）
```

## 5. 损失函数与训练目标

请写出你实现的损失函数与总目标（含 $\lambda$ 的位置说明）：

```text
L_y = ...
L_d = ...
L   = ...      （注意：λ 是内置在 GRL 里，还是乘在 L_d 前面？二者选其一，不要重复）
```

并说明 $\lambda$ 的退火公式与你的具体实现方式：

```text
（在这里填写）
```

## 6. 训练设置

| 配置 | 数值 |
|:---:|:---:|
| epochs | |
| batch size | |
| 每个 batch 中源域 / 目标域比例 | |
| optimizer | Adam / AdamW |
| learning rate | |
| weight decay | |
| LR scheduler | |
| λ 退火参数 γ | |
| loss | L_y + L_d（λ 内置 GRL） |
| device | CPU / GPU |

请说明为何选用这些设置，尤其是 batch 内源域和目标域为何要保持均衡：

```text
（在这里填写）
```

## 7. 训练过程

### 7.1 Loss 与 Accuracy 记录

| Epoch | L_y | 源域 Acc | L_d | 域判别 Acc | 目标域 Acc | λ |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| 1 | | | | | | |
| 2 | | | | | | |
| ... | | | | | | |

请粘贴 label loss、domain loss、λ 调度、源域/目标域 accuracy 曲线。

请描述训练过程是否正常：

- L_y 是否稳定下降？
- L_d 是否最终维持在「无法分清域」的水平（域判别准确率约 0.5 附近）？
- λ 是否按调度逐步上升？
- 目标域（无标签训练数据）的 accuracy 是否随训练逐步上升？

```text
（在这里填写）
```

## 8. 测试结果

### 8.1 逐被试结果（LOSO）

| 目标被试 | DANN Acc | DANN Kappa | source-only Acc |
|:---:|:---:|:---:|:---:|
| A01 | | | |
| A02 | | | |
| ... | | | |
| 平均 | | | |

### 8.2 总体指标

| 指标 | source-only 基线 | 你的 DANN | target-supervised 上界（可选） |
|---|---:|---:|---:|
| 平均 target accuracy | | | |
| 平均 target Kappa | | | |
| random baseline（4 类） | 25% | 25% | 25% |

### 8.3 混淆矩阵

请粘贴（至少一个代表被试的）混淆矩阵，并分析哪两类最容易混淆、为什么。

```text
（在这里填写分析和混淆矩阵图）
```

### 8.4 结果分析

请对比分析：

- DANN 相对 source-only 基线提升了多少？提升是否稳定（跨被试波动大不大）？
- 与文献中报告的跨被试结果对比时，注意其数据假设可能不同（如 Wang et al. 2023 使用了静息态校准数据），你的结果处于什么水平？
- 哪些被试提升明显、哪些几乎没提升？可能的原因是什么？

```text
（在这里填写）
```

## 9. 特征可视化（t-SNE）与分析

这是本次作业的重要环节——验证「域不变」是否真的达成。

### 9.1 适配前后特征对比

请分别画出：
- **source-only 基线**（未做域适配）时 G_f 特征的 t-SNE（按域着色：源域一种颜色、目标域一种颜色）；
- **DANN 训练后** G_f 特征的 t-SNE（同样按域着色）。

如果域适配成功，你应该看到：适配前源域和目标域特征聚成两个明显分离的簇；适配后两者混合在一起。

```text
（在这里粘贴 t-SNE 图）
```

### 9.2 按类着色

再画一张**按类着色**的 t-SNE（只对目标域样本），观察 4 类是否形成可分的簇：

```text
（在这里粘贴 t-SNE 图）
```

### 9.3 分析

- 域适配后源域和目标域特征是否「混合」了？混合程度如何？
- 按类着色是否仍保持 4 类可分（说明在域不变的同时保留了判别性）？
- 有没有出现「负迁移」（negative transfer），即适配后反而更差？如果有，分析原因。

```text
（在这里填写分析）
```

## 10. 消融实验与对比

### 10.1 关键组件消融

| 实验 | 配置 | 平均 target Acc | 平均 Kappa |
|:---:|:---:|:---:|:---:|
| source-only | λ=0（无域判别） | | |
| 完整 DANN | 默认配置 | | |
| 移除 GRL | 直接把 L_d 加入损失但不取反 | | |
| λ 不退火 | λ 恒为 1 | | |
| 不同 γ | γ=5 / γ=20 | | |

### 10.2 Backbone 对比

| Backbone | 平均 target Acc | 平均 Kappa | 备注 |
|:---:|:---:|:---:|:---:|
| EEGNet | | | |
| EEGConformer | | | |

### 10.3 其他实验（可选，自行尝试）

- 源域规模对结果的影响（源域从 8 人逐步减少到 2 人）；
- 不同特征维度 / 不同域判别器深度；
- 在域判别器中加入 dropout 或标签平滑等稳定技巧。

请分析哪个因素对域适配效果影响最大，说明原因：

```text
（在这里填写分析）
```

## 11. 问题与改进

请说明你在复现过程中：

- **遇到了哪些问题**：
  - GRL 反向传播不生效？（自定义 `autograd.Function` 的 `backward` 返回值顺序是否写对）
  - λ 退火实现是否有误？（λ 是否随迭代正确从 0 上升）
  - 域判别器过强导致特征塌缩？batch 内源/目标比例失衡？
  - 训练不收敛时如何排查？

- **如何定位并解决的**：具体描述你的调试过程。

- **对域适配的理解**：做完这次作业后，你对「域偏移」「对抗训练」「域不变特征」有了哪些新的认识？

- **如果继续改进，可以从哪些方面入手**：如引入 MMD / 条件域判别（CDAN）、更强的 backbone、目标域伪标签（pseudo-labeling）、多源域加权、其他对齐方法（如欧氏对齐 EA）等。

```text
（在这里填写）
```

## 12. Git 提交记录

- 仓库地址：
- 总 commit 数：

粘贴 `git log --oneline` 输出：

```text
（在这里粘贴 git log --oneline）
```

## 13. 参考资料

- Ganin, Y., & Lempitsky, V. (2015). Domain-Adversarial Training of Neural Networks. Arxiv Preprint:1505.07818

- 其他你在完成作业过程中参考的资料（博客、教程、开源代码等）：

```text
（在这里列出）
```
